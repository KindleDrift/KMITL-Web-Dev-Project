# Current Database Seed Data

The current seed data includes:
- `Admin` role user with email `admin@tinytender.local` and password `Admin123!`. Display name is set to `Admin`. No other properties are set, so they will be null.
- `Alice`, `Bob`, and `Charlie` as regular users with emails `alice@example.com`, `bob@example.com`, and `charlie@example.com` and passwords `User123!`. Display names are set to `Alice`, `Bob`, and `Charlie` respectively. No other properties are set, so they will be null.
- 2 Post entries created by `Alice` with `Bob` only joining on of them. View details in DbSeeder.cs.