# Current Database Seed Data

The current seed data includes:
- `Admin` role user with email `admin@tinytender.local` and password `Admin123!`. Display name is set to `Admin`. No other properties are set, so they will be null.
- Currently no other users are being seeded. Feel free to add more users for now.